services:
  db:
    image: postgres:alpine
    ports:
      - 5432:5432
    volumes:
      - ./postgres/:/docker-entrypoint-initdb.d
    env_file:
      - ./.env
  redis:
    image: redis:alpine
    ports:
      - 6379:6379
